@page "/counter"
@using CommuteLogger.Components

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<SmartCardModel ChangeReaderNames="OnChangeReaderNames" />
<br />
計算結果:@ReceivedReaderNames

@code {
    private string[] ReceivedReaderNames = Array.Empty<string>();

        private void OnChangeReaderNames(string[] readerNames)
        {
            this.ReceivedReaderNames = readerNames;
        }
    }